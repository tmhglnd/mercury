
// set debug 1

set midi getPorts
set scale minor_harmonic c

ring nts sine(32 16.774 0 24)
ring len cosine(128 1 100 10)

new midi "IAC-besturingsbestand Bus 1" name(midi1) 
	set midi1 note(nts 1) time(1/16) amp(0.9)
	set midi1 length(len) channel(12)

silence
